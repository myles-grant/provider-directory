export interface Provider {
    id: string
    name: string
    title: string
    avatarUrl: string
    availabilty: string
    location: string
    education: string
    languages: string[]
    bio: string
}